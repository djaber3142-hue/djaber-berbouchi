rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global safety net: default-deny catch-all
    match /{document=**} {
      allow read, write: if false;
    }

    // Tournament state rule
    match /tournament_state/{stateId} {
      allow read, write: if true;
    }
  }
}
